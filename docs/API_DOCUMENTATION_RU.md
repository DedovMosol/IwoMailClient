# iwo Mail Client - API Документация

**Версия:** 1.0.6c (26.12.2025)

## Что нового в v1.0.6c (по сравнению с v1.0.5c)

### Новые функции
- **Запрос отчёта о прочтении (MDN)** — при отправке письма можно запросить уведомление о прочтении
- **Запрос отчёта о доставке (DSN)** — подтверждение доставки письма на сервер получателя
- **Настройка размера шрифта** — маленький/средний/большой в настройках
- **Индивидуальные настройки синхронизации** — каждый аккаунт имеет свой режим и интервал
- **Настройки в карточке аккаунта** — режим (Push/Scheduled) и интервал прямо в UI

### Оптимизация батареи
- **Выбор режима синхронизации** — Push или По расписанию для Exchange аккаунтов
- **Ночной режим экономии** — 23:00-7:00 синхронизация каждые 60 мин
- **Адаптивный heartbeat** — автоувеличение интервала Ping (5→15 мин)
- **NetworkCallback** — умная остановка/возобновление при изменении сети
- **Debounce синхронизации** — защита от двойных запросов
- **Умная логика синхронизации** — автоотключение периодической синхронизации для Push аккаунтов

### Исправления
- Исправлена вибрация при выделении писем
- Исправлена подсветка букв в поиске на всех устройствах
- Исправлен скролл при "Выбрать всё"
- Исправлены уведомления — фиксированные ID, не создают спам

### Технические улучшения
- База данных v9 — добавлено поле syncIntervalMinutes в AccountEntity
- SyncWorker.getMinSyncInterval() — централизованная функция получения интервала
- SyncMode.getDisplayName() — локализация режимов синхронизации
- ScaledAlertDialog — корректное масштабирование шрифта во всех диалогах
- Очистка кода — удалены неиспользуемые методы из SettingsRepository

## Что нового в v1.0.5c (по сравнению с v1.0.5b)

### Исправления уведомлений
- Исправлены уведомления на заблокированном экране — добавлен `VISIBILITY_PUBLIC`
- Каналы уведомлений теперь пересоздаются при запуске для применения новых настроек
- Добавлена категория `CATEGORY_EMAIL` для правильной обработки системой
- Канал новых писем теперь имеет `lockscreenVisibility = PUBLIC`

### Исправления перемещения писем
- Исправлен баг восстановления из корзины — теперь письмо возвращается в папку, из которой было удалено
- `originalFolderId` корректно обновляется при перемещении между папками

### Исправления навигации
- Исправлен баг открытия письма из уведомления после смахивания приложения
- Добавлена проверка существования письма перед открытием
- Если письмо не найдено — открываются Входящие с фильтром "Непрочитанные"

### Улучшения совместимости
- Улучшена совместимость TLS на разных устройствах — убран устаревший SSLv3
- Добавлена поддержка TLSv1.3
- Улучшена обработка ошибок SSL/TLS — fallback на дефолты OkHttp
- Добавлены таймауты на синхронизацию (60 сек общий, 30 сек на папку)

## Что нового в v1.0.5b (по сравнению с v1.0.4b)

### Модернизация интерфейса
- **Градиентные TopAppBar** — на всех экранах применён фиолетово-синий градиент
- **Единый стиль карточек папок** — яркие градиенты в едином стиле
- **Gmail-style аватары** — стабильные цвета для отправителей на основе хэша имени
- **Пульсирующая анимация** — кнопка "Поддержать разработчика" с плавным эффектом
- **Тактильная обратная связь** — вибрация при выделении писем
- **Секция "Советы"** — сворачиваемая секция с информацией о бета-функциях
- **Очистка корзины** — функция полной очистки корзины с диалогом подтверждения

### Улучшения синхронизации
- **Надёжная фоновая синхронизация** — AlarmManager fallback работает даже когда приложение убито
- **SyncAlarmReceiver** — теперь синхронизирует напрямую, не полагаясь только на WorkManager
- **Автоперевод уведомления** — foreground notification переводится при смене языка

### Исправления
- **Исправлен баг экрана настройки** — больше нет ложного перехода на экран настройки при запуске
- **Улучшена автосинхронизация** — реактивная синхронизация на основе состояния активного аккаунта
- **Навигация из уведомления** — кнопка "назад" из письма ведёт во Входящие, а не на главный экран
- **Сохранение состояния секций** — раскрытые секции не сворачиваются при синхронизации

### Известные ограничения
- IMAP и POP3 в бета-режиме
- Полная поддержка нескольких аккаунтов — в следующих версиях

## Что нового в v1.0.4b (по сравнению с v1.0.3b)

### Оптимизация памяти
- **SettingsRepository singleton** — теперь создаётся один экземпляр на всё приложение вместо создания нового при каждом обращении
- **Кэширование EasClient** — клиент кэшируется для каждого аккаунта, повторные запросы используют существующий экземпляр
- **Исправлена утечка памяти в SyncAlarmReceiver** — coroutine scope теперь локальный с обязательным cancel() в finally блоке

### Оптимизация батареи
- **Убрано дублирование синхронизации** — когда сервер не поддерживает Direct Push, синхронизация больше не запускается дважды (PushService + SyncWorker)

### Production оптимизации
- **Убраны все Log.d() вызовы** — отладочные логи удалены из PushService, SyncWorker, SyncAlarmReceiver
- **Оптимизированы ProGuard правила** — добавлены правила для JavaMail, BouncyCastle, Security Crypto

### Улучшения интерфейса
- **Улучшен диалог "Переместить"** — из системных папок показываются только пользовательские, из пользовательских — все папки
- **Исправлен краш при открытии письма из уведомления** — на заблокированном экране приложение больше не падает при нажатии "назад"
- **IMAP/POP3 помечены как beta** — в интерфейсе добавления аккаунта

## Обзор архитектуры

Приложение поддерживает несколько протоколов для работы с почтовыми серверами:

```
┌─────────────────────────────────────────────────────────────┐
│                      UI Layer                                │
│  MainScreen → EmailListScreen → EmailDetailScreen           │
│  SettingsScreen, SetupScreen, SearchScreen, ComposeScreen   │
└─────────────────────────────────────────────────────────────┘
                            │
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                   Repository Layer                           │
│  AccountRepository ←→ MailRepository ←→ SettingsRepository  │
└─────────────────────────────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        ▼                   ▼                   ▼
┌───────────────┐   ┌───────────────┐   ┌───────────────┐
│   EasClient   │   │  ImapClient   │   │  Pop3Client   │
│  (Exchange)   │   │   (IMAP)      │   │   (POP3)      │
└───────────────┘   └───────────────┘   └───────────────┘
        │                   │
        ▼                   ▼
┌───────────────┐   ┌───────────────┐
│  WbxmlParser  │   │  SmtpClient   │
│ (EAS Binary)  │   │  (Отправка)   │
└───────────────┘   └───────────────┘
        │
        ▼
┌───────────────────────────────────────────────────────────┐
│              Background Services                           │
│  PushService (Direct Push) ←→ SyncWorker (WorkManager)    │
│                    BootReceiver                            │
└───────────────────────────────────────────────────────────┘
```

---

## Фоновая синхронизация

### PushService (Direct Push)

Foreground Service для мгновенных уведомлений о новых письмах через Exchange Direct Push (Ping команда).

**Особенности:**
- Автоматический перезапуск при закрытии приложения
- Адаптивный heartbeat (8-28 минут)
- Автоматическая синхронизация папок при первом запуске
- Умные уведомления (одно письмо → открывает его, несколько → фильтр "Непрочитанные")
- Heads-up уведомления с высоким приоритетом

```kotlin
// Запуск сервиса
PushService.start(context)

// Остановка сервиса
PushService.stop(context)
```

### SyncWorker (WorkManager)

Периодическая фоновая синхронизация через WorkManager.

```kotlin
// Планирование синхронизации
SyncWorker.schedule(
    context,
    intervalMinutes = 15,
    wifiOnly = false
)

// Немедленная синхронизация
SyncWorker.syncNow(context)

// Отмена синхронизации
SyncWorker.cancel(context)
```

### SyncAlarmReceiver

BroadcastReceiver для надёжной синхронизации через AlarmManager. Работает даже когда PushService убит системой (Xiaomi/MIUI).

```kotlin
// Автоматически планируется PushService когда приложение свайпнуто
// Запускает синхронизацию и перезапускает PushService
```

### SettingsRepository - Отслеживание уведомлений

```kotlin
// Время последней проверки уведомлений - используется для определения новых писем
val lastNotificationCheckTime: Flow<Long>
suspend fun setLastNotificationCheckTime(timeMillis: Long)
fun getLastNotificationCheckTimeSync(): Long
```

### EmailDao - Определение новых писем

```kotlin
// Получить непрочитанные письма, полученные после указанного времени (для уведомлений)
suspend fun getNewUnreadEmails(accountId: Long, afterTime: Long): List<EmailEntity>
```

---

## Локализация

Приложение поддерживает два языка:
- 🇷🇺 Русский 
- 🇬🇧 English (по умолчанию)

### Localization.kt
```kotlin
enum class AppLanguage(val code: String, val displayName: String) {
    RUSSIAN("ru", "🇷🇺 Русский"),
    ENGLISH("en", "🇬🇧 English")
}

object Strings {
    // Названия папок
    val inbox: String @Composable get() = if (isRussian()) "Входящие" else "Inbox"
    val drafts: String @Composable get() = if (isRussian()) "Черновики" else "Drafts"
    val sent: String @Composable get() = if (isRussian()) "Отправленные" else "Sent"
    val trash: String @Composable get() = if (isRussian()) "Удалённые" else "Trash"
    val spam: String @Composable get() = if (isRussian()) "Спам" else "Spam"
    
    // Действия
    val movedToTrash: String @Composable get() = if (isRussian()) "Перемещено в корзину" else "Moved to trash"
    val deletedPermanently: String @Composable get() = if (isRussian()) "Удалено окончательно" else "Deleted permanently"
    val restored: String @Composable get() = if (isRussian()) "Восстановлено" else "Restored"
    
    // ComposeScreen
    val scheduleSend: String @Composable get() = if (isRussian()) "Запланировать отправку" else "Schedule send"
    val tomorrowMorning: String @Composable get() = if (isRussian()) "Завтра утром" else "Tomorrow morning"
    val selectDateTime: String @Composable get() = if (isRussian()) "Выбрать дату и время" else "Pick date & time"
    
    // Локализация названия папки по типу
    @Composable
    fun getFolderName(type: Int, originalName: String): String
}
```

### NotificationStrings
Локализация для уведомлений и Toast (вне Compose контекста):
```kotlin
object NotificationStrings {
    fun getNewMailTitle(isRussian: Boolean) = 
        if (isRussian) "Новая почта" else "New mail"
    
    fun getMovedToTrash(isRussian: Boolean) =
        if (isRussian) "Перемещено в корзину" else "Moved to trash"
    
    fun getRestored(isRussian: Boolean) =
        if (isRussian) "Восстановлено" else "Restored"
    
    fun localizeError(errorCode: String, isRussian: Boolean): String
}
```

---

## MailRepository API

### Синхронизация
```kotlin
suspend fun syncFolders(accountId: Long): EasResult<Unit>
suspend fun syncEmails(accountId: Long, folderId: String): EasResult<Int>
```

### Перемещение и удаление писем
```kotlin
// Перемещение в произвольную папку
suspend fun moveEmails(emailIds: List<String>, targetFolderId: String): EasResult<Int>

// Перемещение в спам
suspend fun moveToSpam(emailIds: List<String>): EasResult<Int>

// Перемещение в корзину или окончательное удаление
suspend fun moveToTrash(emailIds: List<String>): EasResult<Int>

// Восстановление из корзины в исходную папку
suspend fun restoreFromTrash(emailIds: List<String>): EasResult<Int>

// Окончательное удаление
suspend fun deleteEmailsPermanently(emailIds: List<String>): EasResult<Int>
```

### Управление папками
```kotlin
suspend fun createFolder(accountId: Long, folderName: String): EasResult<Unit>
suspend fun deleteFolder(accountId: Long, folderId: String): EasResult<Unit>
suspend fun renameFolder(accountId: Long, folderId: String, newName: String): EasResult<Unit>
```

---

## EasClient API

### Отправка писем
```kotlin
// Отправка простого письма
suspend fun sendMail(
    to: String,
    subject: String,
    body: String,
    cc: String = "",
    importance: Int = 1,
    requestReadReceipt: Boolean = false,    // MDN - запрос отчёта о прочтении
    requestDeliveryReceipt: Boolean = false // DSN - запрос отчёта о доставке
): EasResult<Boolean>

// Отправка письма с вложениями
suspend fun sendMailWithAttachments(
    to: String,
    subject: String,
    body: String,
    cc: String = "",
    attachments: List<Triple<String, String, ByteArray>>, // name, mimeType, data
    requestReadReceipt: Boolean = false,
    requestDeliveryReceipt: Boolean = false
): EasResult<Boolean>
```

### MIME заголовки для отчётов
```
// MDN (Message Disposition Notification) - отчёт о прочтении
Disposition-Notification-To: sender@example.com
Return-Receipt-To: sender@example.com
X-Confirm-Reading-To: sender@example.com

// DSN (Delivery Status Notification) - отчёт о доставке
X-MS-Exchange-Organization-DeliveryReportRequested: true
Return-Path: sender@example.com
```

---

## База данных

### AccountEntity
```kotlin
data class AccountEntity(
    val id: Long,
    val email: String,
    val displayName: String,
    val serverUrl: String,
    val username: String,
    val domain: String,
    val accountType: String,      // EXCHANGE, IMAP, POP3
    val syncMode: String,         // PUSH, SCHEDULED (v1.0.6c+)
    val acceptAllCerts: Boolean,
    val color: Int
)
```

### SyncMode (v1.0.6c+)
```kotlin
enum class SyncMode {
    PUSH,      // Exchange Direct Push - мгновенные уведомления
    SCHEDULED  // Периодическая синхронизация по расписанию
}
```

### EmailEntity
```kotlin
data class EmailEntity(
    val id: String,
    val accountId: Long,
    val folderId: String,
    val serverId: String,
    val from: String,
    val fromName: String,
    val to: String,
    val cc: String,
    val subject: String,
    val preview: String,
    val body: String,
    val bodyType: Int,
    val dateReceived: Long,
    val read: Boolean,
    val flagged: Boolean,
    val importance: Int,
    val hasAttachments: Boolean,
    val originalFolderId: String? // Исходная папка до перемещения в корзину
)
```

---

## Коды ошибок

### HTTP
| Code | Описание |
|------|----------|
| 200 | OK |
| 401 | Unauthorized |
| 403 | Forbidden |
| 449 | Retry (Provision) |
| 500 | Server Error |

### EAS Status
| Status | Описание |
|--------|----------|
| 1 | Success |
| 3 | Invalid SyncKey |
| 12 | Object not found |
| 141 | Device not provisioned |
